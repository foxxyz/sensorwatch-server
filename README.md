Sensor Watch Web Monitor
========================

Web Monitor for showing sensorwatch data.

Requirements
------------

 * Node.js (install package `nodejs` with your favorite package manager)

Installation
------------

1. Clone project: `git clone <url>`
2. Install dependencies: `npm install`
